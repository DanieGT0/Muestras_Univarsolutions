# ==========================================
# VARIABLES DE ENTORNO PARA RENDER
# ==========================================
# Copiar estas variables exactamente en Render Dashboard
# Backend → Environment → Add Environment Variable

# 1. DATABASE_URL (Neon PostgreSQL - CRÍTICO)
DATABASE_URL=postgresql://neondb_owner:npg_Ez8jRiUutQ0W@ep-noisy-truth-afxypmn4-pooler.c-2.us-west-2.aws.neon.tech/Muestras?sslmode=require

# 2. JWT_SECRET (CRÍTICO - Usar el mismo o generar uno nuevo)
JWT_SECRET=tu-clave-secreta-jwt-super-segura-cambiar-en-produccion

# 3. FRONTEND_URL (CRÍTICO - Reemplazar con tu URL real)
FRONTEND_URL=https://muestras-univarsolutions-frontend.onrender.com

# 4. NODE_ENV (CRÍTICO)
NODE_ENV=production

# 5. PORT (OPCIONAL - Render asigna automáticamente)
PORT=3001

# ==========================================
# INSTRUCCIONES:
# ==========================================
# 1. Ve a: https://dashboard.render.com
# 2. Selecciona tu Backend service
# 3. Ve a Environment
# 4. Agrega cada variable UNA POR UNA
# 5. Click "Save Changes"
# 6. Espera el redespliegue automático
#
# IMPORTANTE:
# - NO agregues comillas a los valores
# - NO agregues espacios antes/después
# - Copia DATABASE_URL EXACTAMENTE como está
# - Debe incluir ?sslmode=require al final
